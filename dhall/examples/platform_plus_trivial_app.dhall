-- Phase 33's oracle-pinned live-deploy input.  The standard platform is the
-- already-materialized Phase 30/31 representative set; this Deployment value
-- adds the deliberately trivial application and names the exact typed live
-- pipeline input.  The live gate, not this comment, proves its enact set.
./legal_deployment_rules.dhall
