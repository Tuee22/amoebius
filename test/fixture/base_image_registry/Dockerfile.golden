# syntax=docker/dockerfile:1.7
# GENERATED BY Amoebius.Image.RenderDockerfile FROM dhall/amoebius/BakeCatalog.dhall; DO NOT EDIT
FROM ubuntu:24.04@sha256:561618e2c15bf2397621dd04f96926663a3b5616c189cf7e38db7e82f5c538ea AS amoebius-base
ARG TARGETARCH
LABEL org.opencontainers.image.title="amoebius monocontainer"
LABEL io.amoebius.bake-catalog="dhall/amoebius/BakeCatalog.dhall"
RUN apt-get update \
    && apt-get install -y --no-install-recommends \
         ca-certificates=20260601~24.04.1 \
         curl=8.5.0-2ubuntu10.11 \
         unzip=6.0-28ubuntu4.1 \
         docker-registry=2.8.2+ds1-1ubuntu0.24.04.3 \
         redis-server=5:7.0.15-1ubuntu0.24.04.4 \
         redis-tools=5:7.0.15-1ubuntu0.24.04.4 \
         postgresql-16=16.14-0ubuntu0.24.04.1 \
         patroni=3.2.2-2 \
         openjdk-21-jre-headless=21.0.11+10-1~24.04.2 \
         g++=4:13.2.0-7ubuntu1 \
    && rm -rf /var/lib/apt/lists/*
RUN set -eu \
    && url="$(case ${TARGETARCH} in amd64) echo https://dl.min.io/server/minio/release/linux-amd64/archive/minio.RELEASE.2025-07-23T15-54-02Z ;; arm64) echo https://dl.min.io/server/minio/release/linux-arm64/archive/minio.RELEASE.2025-07-23T15-54-02Z ;; esac)" \
    && sums="$(case ${TARGETARCH} in amd64) echo https://dl.min.io/server/minio/release/linux-amd64/archive/minio.RELEASE.2025-07-23T15-54-02Z.sha256sum ;; arm64) echo https://dl.min.io/server/minio/release/linux-arm64/archive/minio.RELEASE.2025-07-23T15-54-02Z.sha256sum ;; esac)" \
    && asset="${url##*/}" \
    && curl -fsSL -o /tmp/asset "${url}" \
    && curl -fsSL -o /tmp/asset.sums "${sums}" \
    && test "$(sha256sum /tmp/asset | cut -d' ' -f1)" = "$(grep -E "[ /]${asset}$" /tmp/asset.sums | head -n1 | cut -d' ' -f1)" \
    && install -m 0755 /tmp/asset /usr/bin/minio \
    && rm -f /tmp/asset /tmp/asset.sums
RUN set -eu \
    && url="$(case ${TARGETARCH} in amd64) echo https://releases.hashicorp.com/vault/1.20.2/vault_1.20.2_linux_amd64.zip ;; arm64) echo https://releases.hashicorp.com/vault/1.20.2/vault_1.20.2_linux_arm64.zip ;; esac)" \
    && sums="$(case ${TARGETARCH} in amd64) echo https://releases.hashicorp.com/vault/1.20.2/vault_1.20.2_SHA256SUMS ;; arm64) echo https://releases.hashicorp.com/vault/1.20.2/vault_1.20.2_SHA256SUMS ;; esac)" \
    && asset="${url##*/}" \
    && curl -fsSL -o /tmp/asset "${url}" \
    && curl -fsSL -o /tmp/asset.sums "${sums}" \
    && test "$(sha256sum /tmp/asset | cut -d' ' -f1)" = "$(grep -E "[ /]${asset}$" /tmp/asset.sums | head -n1 | cut -d' ' -f1)" \
    && unzip -p /tmp/asset vault > /usr/bin/vault && chmod 0755 /usr/bin/vault \
    && rm -f /tmp/asset /tmp/asset.sums
RUN set -eu \
    && url="$(case ${TARGETARCH} in amd64) echo https://github.com/prometheus/prometheus/releases/download/v3.5.0/prometheus-3.5.0.linux-amd64.tar.gz ;; arm64) echo https://github.com/prometheus/prometheus/releases/download/v3.5.0/prometheus-3.5.0.linux-arm64.tar.gz ;; esac)" \
    && sums="$(case ${TARGETARCH} in amd64) echo https://github.com/prometheus/prometheus/releases/download/v3.5.0/sha256sums.txt ;; arm64) echo https://github.com/prometheus/prometheus/releases/download/v3.5.0/sha256sums.txt ;; esac)" \
    && asset="${url##*/}" \
    && curl -fsSL -o /tmp/asset "${url}" \
    && curl -fsSL -o /tmp/asset.sums "${sums}" \
    && test "$(sha256sum /tmp/asset | cut -d' ' -f1)" = "$(grep -E "[ /]${asset}$" /tmp/asset.sums | head -n1 | cut -d' ' -f1)" \
    && tar -xzf /tmp/asset --wildcards -O "*/prometheus" > /usr/bin/prometheus && chmod 0755 /usr/bin/prometheus \
    && rm -f /tmp/asset /tmp/asset.sums
RUN set -eu \
    && url="$(case ${TARGETARCH} in amd64) echo https://github.com/prometheus/alertmanager/releases/download/v0.28.1/alertmanager-0.28.1.linux-amd64.tar.gz ;; arm64) echo https://github.com/prometheus/alertmanager/releases/download/v0.28.1/alertmanager-0.28.1.linux-arm64.tar.gz ;; esac)" \
    && sums="$(case ${TARGETARCH} in amd64) echo https://github.com/prometheus/alertmanager/releases/download/v0.28.1/sha256sums.txt ;; arm64) echo https://github.com/prometheus/alertmanager/releases/download/v0.28.1/sha256sums.txt ;; esac)" \
    && asset="${url##*/}" \
    && curl -fsSL -o /tmp/asset "${url}" \
    && curl -fsSL -o /tmp/asset.sums "${sums}" \
    && test "$(sha256sum /tmp/asset | cut -d' ' -f1)" = "$(grep -E "[ /]${asset}$" /tmp/asset.sums | head -n1 | cut -d' ' -f1)" \
    && tar -xzf /tmp/asset --wildcards -O "*/alertmanager" > /usr/bin/alertmanager && chmod 0755 /usr/bin/alertmanager \
    && rm -f /tmp/asset /tmp/asset.sums
RUN set -eu \
    && url="$(case ${TARGETARCH} in amd64) echo https://github.com/thanos-io/thanos/releases/download/v0.39.2/thanos-0.39.2.linux-amd64.tar.gz ;; arm64) echo https://github.com/thanos-io/thanos/releases/download/v0.39.2/thanos-0.39.2.linux-arm64.tar.gz ;; esac)" \
    && sums="$(case ${TARGETARCH} in amd64) echo https://github.com/thanos-io/thanos/releases/download/v0.39.2/sha256sums.txt ;; arm64) echo https://github.com/thanos-io/thanos/releases/download/v0.39.2/sha256sums.txt ;; esac)" \
    && asset="${url##*/}" \
    && curl -fsSL -o /tmp/asset "${url}" \
    && curl -fsSL -o /tmp/asset.sums "${sums}" \
    && test "$(sha256sum /tmp/asset | cut -d' ' -f1)" = "$(grep -E "[ /]${asset}$" /tmp/asset.sums | head -n1 | cut -d' ' -f1)" \
    && tar -xzf /tmp/asset --wildcards -O "*/thanos" > /usr/bin/thanos && chmod 0755 /usr/bin/thanos \
    && rm -f /tmp/asset /tmp/asset.sums
RUN set -eu \
    && url="$(case ${TARGETARCH} in amd64) echo https://github.com/envoyproxy/envoy/releases/download/v1.35.1/envoy-1.35.1-linux-x86_64 ;; arm64) echo https://github.com/envoyproxy/envoy/releases/download/v1.35.1/envoy-1.35.1-linux-aarch_64 ;; esac)" \
    && sums="$(case ${TARGETARCH} in amd64) echo https://github.com/envoyproxy/envoy/releases/download/v1.35.1/checksums.txt.asc ;; arm64) echo https://github.com/envoyproxy/envoy/releases/download/v1.35.1/checksums.txt.asc ;; esac)" \
    && asset="${url##*/}" \
    && curl -fsSL -o /tmp/asset "${url}" \
    && curl -fsSL -o /tmp/asset.sums "${sums}" \
    && test "$(sha256sum /tmp/asset | cut -d' ' -f1)" = "$(grep -E "[ /]${asset}$" /tmp/asset.sums | head -n1 | cut -d' ' -f1)" \
    && install -m 0755 /tmp/asset /usr/bin/envoy \
    && rm -f /tmp/asset /tmp/asset.sums
RUN set -eu \
    && url="$(case ${TARGETARCH} in amd64) echo https://dl.grafana.com/oss/release/grafana-12.1.1.linux-amd64.tar.gz ;; arm64) echo https://dl.grafana.com/oss/release/grafana-12.1.1.linux-arm64.tar.gz ;; esac)" \
    && sums="$(case ${TARGETARCH} in amd64) echo https://dl.grafana.com/oss/release/grafana-12.1.1.linux-amd64.tar.gz.sha256 ;; arm64) echo https://dl.grafana.com/oss/release/grafana-12.1.1.linux-arm64.tar.gz.sha256 ;; esac)" \
    && asset="${url##*/}" \
    && curl -fsSL -o /tmp/asset "${url}" \
    && curl -fsSL -o /tmp/asset.sums "${sums}" \
    && test "$(sha256sum /tmp/asset | cut -d' ' -f1)" = "$(cut -d' ' -f1 /tmp/asset.sums)" \
    && mkdir -p /usr/share/grafana && tar -xzf /tmp/asset --strip-components=1 -C /usr/share/grafana \
    && rm -f /tmp/asset /tmp/asset.sums
RUN set -eu \
    && url="$(case ${TARGETARCH} in amd64) echo https://github.com/keycloak/keycloak/releases/download/26.3.2/keycloak-26.3.2.tar.gz ;; arm64) echo https://github.com/keycloak/keycloak/releases/download/26.3.2/keycloak-26.3.2.tar.gz ;; esac)" \
    && sums="$(case ${TARGETARCH} in amd64) echo https://github.com/keycloak/keycloak/releases/download/26.3.2/keycloak-26.3.2.tar.gz.sha1 ;; arm64) echo https://github.com/keycloak/keycloak/releases/download/26.3.2/keycloak-26.3.2.tar.gz.sha1 ;; esac)" \
    && asset="${url##*/}" \
    && curl -fsSL -o /tmp/asset "${url}" \
    && curl -fsSL -o /tmp/asset.sums "${sums}" \
    && test "$(sha1sum /tmp/asset | cut -d' ' -f1)" = "$(cut -d' ' -f1 /tmp/asset.sums)" \
    && mkdir -p /opt/keycloak && tar -xzf /tmp/asset --strip-components=1 -C /opt/keycloak \
    && rm -f /tmp/asset /tmp/asset.sums
RUN set -eu \
    && url="$(case ${TARGETARCH} in amd64) echo https://archive.apache.org/dist/pulsar/pulsar-4.0.6/apache-pulsar-4.0.6-bin.tar.gz ;; arm64) echo https://archive.apache.org/dist/pulsar/pulsar-4.0.6/apache-pulsar-4.0.6-bin.tar.gz ;; esac)" \
    && sums="$(case ${TARGETARCH} in amd64) echo https://downloads.apache.org/pulsar/pulsar-4.0.6/apache-pulsar-4.0.6-bin.tar.gz.sha512 ;; arm64) echo https://downloads.apache.org/pulsar/pulsar-4.0.6/apache-pulsar-4.0.6-bin.tar.gz.sha512 ;; esac)" \
    && asset="${url##*/}" \
    && curl -fsSL -o /tmp/asset "${url}" \
    && curl -fsSL -o /tmp/asset.sums "${sums}" \
    && test "$(sha512sum /tmp/asset | cut -d' ' -f1)" = "$(grep -E "[ /]${asset}$" /tmp/asset.sums | head -n1 | cut -d' ' -f1)" \
    && mkdir -p /pulsar && tar -xzf /tmp/asset --strip-components=1 -C /pulsar \
    && rm -f /tmp/asset /tmp/asset.sums
RUN set -eu \
    && url="$(case ${TARGETARCH} in amd64) echo https://archive.apache.org/dist/pulsar/pulsar-4.0.6/apache-pulsar-offloaders-4.0.6-bin.tar.gz ;; arm64) echo https://archive.apache.org/dist/pulsar/pulsar-4.0.6/apache-pulsar-offloaders-4.0.6-bin.tar.gz ;; esac)" \
    && sums="$(case ${TARGETARCH} in amd64) echo https://archive.apache.org/dist/pulsar/pulsar-4.0.6/apache-pulsar-offloaders-4.0.6-bin.tar.gz.sha512 ;; arm64) echo https://archive.apache.org/dist/pulsar/pulsar-4.0.6/apache-pulsar-offloaders-4.0.6-bin.tar.gz.sha512 ;; esac)" \
    && asset="${url##*/}" \
    && curl -fsSL -o /tmp/asset "${url}" \
    && curl -fsSL -o /tmp/asset.sums "${sums}" \
    && test "$(sha512sum /tmp/asset | cut -d' ' -f1)" = "$(grep -E "[ /]${asset}$" /tmp/asset.sums | head -n1 | cut -d' ' -f1)" \
    && mkdir -p /pulsar && tar -xzf /tmp/asset --strip-components=1 -C /pulsar \
    && rm -f /tmp/asset /tmp/asset.sums
COPY --link out/${TARGETARCH}/amoebius-jit-build-resolver /usr/bin/amoebius
COPY --link out/${TARGETARCH}/envoy-gateway /usr/bin/envoy-gateway
COPY --link out/${TARGETARCH}/metallb-controller /usr/bin/metallb-controller
COPY --link out/${TARGETARCH}/metallb-speaker /usr/bin/metallb-speaker
COPY --link out/${TARGETARCH}/percona-postgresql-operator /usr/bin/percona-postgresql-operator
COPY --link out/${TARGETARCH}/pgadmin /pgadmin4
ENV JAVA_HOME="/usr/lib/jvm/java-21-openjdk-${TARGETARCH}"
ENV PATH="/usr/lib/jvm/java-21-openjdk-${TARGETARCH}/bin:/usr/lib/postgresql/16/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin"
ENV PULSAR_LOG_DIR="/tmp"
USER 65532:65532
ENTRYPOINT ["/usr/bin/amoebius"]
