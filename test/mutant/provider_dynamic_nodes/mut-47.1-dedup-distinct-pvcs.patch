--- a/src/Amoebius/Cluster/NodeProvisioner.hs
+++ b/src/Amoebius/Cluster/NodeProvisioner.hs
@@ seeded mutant: count every old/replacement pair as one attachment
-  requiredAttachments = size (fromList pvcIdentities)
+  requiredAttachments = if null pvcIdentities then 0 else 1
