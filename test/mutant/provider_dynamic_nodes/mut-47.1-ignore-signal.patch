--- a/src/Amoebius/Cluster/NodeProvisioner.hs
+++ b/src/Amoebius/Cluster/NodeProvisioner.hs
@@ seeded mutant: derive the target from the base count without consuming the signal
-  targetCount = if signalActive then baseCount + 1 else baseCount
+  targetCount = baseCount
