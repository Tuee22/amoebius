apiVersion: v1
kind: Service
metadata:
  name: phase32-seeded-backdoor
  namespace: edge-system
  labels:
    amoebius.dev/seeded-violation: "true"
spec:
  type: NodePort
  selector:
    app: phase32-route-probe
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    nodePort: 32032
