# Amoebius Engineering Doctrine

> **Purpose**: Index of amoebius engineering and architecture doctrine, enumerating the complete
> Phase 0 documentation suite — the whole DSL, specified before implementation.
> **Read this if**: a subsystem needs its owning doctrine, or a new doctrine document needs a home in the set.

This index lists the engineering doctrine set by subject and names what each document owns. It carries no
doctrine itself: every entry's authority is its target, and a summary here that disagrees with the document it
names is a defect in this index. Reading order across the set is owned by
[reading_order.md](../reading_order.md), and delivery sequencing by
[`../../DEVELOPMENT_PLAN/README.md`](../../DEVELOPMENT_PLAN/README.md).

<details>
<summary>Link-graph metadata</summary>

**Status**: Reference only
**Supersedes**: N/A
**Referenced by**: DEVELOPMENT_PLAN/README.md, DEVELOPMENT_PLAN/overview.md, DEVELOPMENT_PLAN/phase_48_test_workflow_algebra.md, DEVELOPMENT_PLAN/phase_60_retained_storage.md, DEVELOPMENT_PLAN/phase_62_platform_backbone.md, DEVELOPMENT_PLAN/phase_63_platform_services_2.md, DEVELOPMENT_PLAN/phase_69_content_store_workflow.md, DEVELOPMENT_PLAN/phase_71_release_lifecycle.md, DEVELOPMENT_PLAN/phase_76_provider_deploy_checkpoint.md, DEVELOPMENT_PLAN/phase_77_provider_child_bringup.md, DEVELOPMENT_PLAN/phase_78_provider_ebs_credential.md, DEVELOPMENT_PLAN/phase_79_provider_dynamic_nodes.md, DEVELOPMENT_PLAN/phase_80_determinism_jitcache.md, DEVELOPMENT_PLAN/phase_89_apple_metal_host_daemon.md, DEVELOPMENT_PLAN/substrates.md, documents/README.md, documents/documentation_standards.md, documents/engineering/app_vs_deployment_doctrine.md, documents/engineering/apple_metal_headless_builds.md, documents/engineering/backup_recovery_doctrine.md, documents/engineering/bootstrap_sequence_doctrine.md, documents/engineering/capability_extension_doctrine.md, documents/engineering/chaos_failover_doctrine.md, documents/engineering/chaos_failover_second_axis.md, documents/engineering/chaos_failover_worked_examples.md, documents/engineering/cluster_lifecycle_doctrine.md, documents/engineering/cluster_topology_doctrine.md, documents/engineering/content_addressing_determinism.md, documents/engineering/content_addressing_doctrine.md, documents/engineering/daemon_topology_doctrine.md, documents/engineering/deterministic_simulation_doctrine.md, documents/engineering/diagram_conventions.md, documents/engineering/dsl_doctrine.md, documents/engineering/evidence_calculus_doctrine.md, documents/engineering/extension_conformance_doctrine.md, documents/engineering/formal_model_doctrine.md, documents/engineering/gateway_migration_model_doctrine.md, documents/engineering/host_cluster_comms_doctrine.md, documents/engineering/image_build_doctrine.md, documents/engineering/inforcespec_migration_doctrine.md, documents/engineering/jit_artifact_doctrine.md, documents/engineering/jit_budget_doctrine.md, documents/engineering/lift_and_compose_doctrine.md, documents/engineering/low_code_ui_runtime_doctrine.md, documents/engineering/low_code_ui_workflow_lifting.md, documents/engineering/manifest_generation_doctrine.md, documents/engineering/migration_doctrine.md, documents/engineering/namespace_layout_doctrine.md, documents/engineering/network_fabric_doctrine.md, documents/engineering/platform_services_doctrine.md, documents/engineering/preflight_validation_doctrine.md, documents/engineering/pulsar_client_doctrine.md, documents/engineering/pulumi_ebs_credential_model.md, documents/engineering/pulumi_iac_doctrine.md, documents/engineering/readiness_ordering_doctrine.md, documents/engineering/release_lifecycle_doctrine.md, documents/engineering/resource_capacity_construction.md, documents/engineering/resource_capacity_doctrine.md, documents/engineering/resource_capacity_folds.md, documents/engineering/resource_capacity_schema.md, documents/engineering/resource_capacity_sources.md, documents/engineering/resource_capacity_storage.md, documents/engineering/resource_capacity_types.md, documents/engineering/service_capability_doctrine.md, documents/engineering/single_logical_data_plane_doctrine.md, documents/engineering/storage_lifecycle_doctrine.md, documents/engineering/substrate_doctrine.md, documents/engineering/tenancy_doctrine.md, documents/engineering/testing_doctrine.md, documents/engineering/tla_modelling_assumptions.md, documents/engineering/vault_pki_doctrine.md, documents/engineering/workflow_calculus_doctrine.md, documents/glossary.md, documents/illegal_state/README.md, documents/illegal_state/illegal_state_techniques.md, documents/reading_order.md
**Generated sections**: none

</details>

---

Phase order, status, and validation gates live only in
[`DEVELOPMENT_PLAN/README.md`](../../DEVELOPMENT_PLAN/README.md). The documents below are stable doctrine;
when the plan changes the supported design, the affected doctrine is updated in the same change.

All docs follow [documentation_standards.md](../documentation_standards.md). The `Document role` column is not
a validation or delivery status and deliberately uses no phase-status symbols: `Authoritative target` means
the linked document owns design intent, `Draft target` means its authoring is incomplete, `Supporting slice`
means authority remains in a linked hub, and `Redirect only` means the file is superseded. Phase and sprint
status lives solely in [`../../DEVELOPMENT_PLAN/README.md`](../../DEVELOPMENT_PLAN/README.md); no row here can
claim implementation, evidence, a gate pass, validation, or Done.

This is one of three doctrine families under [`documents/`](../README.md). The **illegal-state catalog family**
lives in its own sibling folder, [`../illegal_state/`](../illegal_state/illegal_state_catalog.md); the
**extension contract** is the open-core family, hubbed at
[`extension_conformance_doctrine.md`](./extension_conformance_doctrine.md).

## The DSL (the core)

| Document | Purpose | Document role |
|----------|---------|--|
| [dsl_doctrine.md](./dsl_doctrine.md) | The amoebius DSL boundary: external, untracked Dhall values carry parameters while version-controlled Haskell owns logic, schemas, decoding, gates, and generation; closed illegal shapes are unrepresentable, constructible value/target failures such as capacity are rejected by total decode/provision checks, and only opaque `ProvisionedSpec` reaches `renderAll` | Authoritative target |
| [preflight_validation_doctrine.md](./preflight_validation_doctrine.md) | The pure-functional `Check` validation algebra that is the mechanism of the `dhall update` admission gate: the free GADT with a short-circuit `Bind` monad and accumulating `AllOf`/`Both` combinators, the credential/host/quota probe instances (AWS `DryRun` + STS join, SSH reach + hardware match) and the worst-case dynamic envelope that validates a future-needed credential now, and the recursive-forest `SubtreeValidated` proof tree whose product structure makes a grandchild-ack-then-child-nack unrepresentable | Authoritative target |
| [app_vs_deployment_doctrine.md](./app_vs_deployment_doctrine.md) | The hard split between **application logic** (external `UiSource` values plus Haskell-declared typed ports/workflows, auth references, and durable-data needs) and **deployment rules** (replicas, placement, chaos testing, geo-replication, failover) — write the app once, configure its distribution separately | Authoritative target |
| [low_code_ui_runtime_doctrine.md](./low_code_ui_runtime_doctrine.md) | The bounded external `UiSource` language, checked Haskell `BoundUiProgram`, Haskell-declared client runtime generated lazily as PureScript, same-binary UI-server worker, typed port/effect boundary, isolation, rollout, and HA boundary | Authoritative target |
| [low_code_ui_workflow_lifting.md](./low_code_ui_workflow_lifting.md) | How workflows and their artifacts are lifted into the low-code UX. A slice of the doctrine above. | Supporting slice |
| [browser_offline_runtime_doctrine.md](./browser_offline_runtime_doctrine.md) | Explicit `OnlineOnly \| Offline` application semantics, bounded queue/blob contracts, paired client/replay plans, encrypted partitioned browser stores, fenced cross-tab ownership, current-authority replay with durable receipts, and finite release compatibility | Authoritative target |
| [illegal-state catalog family](../illegal_state/illegal_state_catalog.md) | **Now its own family in [`documents/illegal_state/`](../illegal_state/illegal_state_catalog.md)** — the [index](../illegal_state/illegal_state_catalog.md) (the map of illegal states a valid `InForceSpec` cannot represent), nine themed sub-catalogs, and the [techniques doc](../illegal_state/illegal_state_techniques.md) (the nine typing techniques + coverage matrix + foreclosure layers + validation-locus axis) | Authoritative target |
| [service_capability_doctrine.md](./service_capability_doctrine.md) | Services as abstract **capabilities** — eight cluster-invariant (ObjectStore · SecretStore · MessageBus · Sql · Identity · Observability · Registry · Edge) plus the extension-provided `InferenceEngine`, nine arms in the closed union — each binds to its doctrine-fixed provider and per-cluster deployment **shape**; extension is capability-specific, and Registry is closed to Distribution `registry:2` | Authoritative target |
| [capability_extension_doctrine.md](./capability_extension_doctrine.md) | The capability-extension graph: `extRequires` dependency declarations and the total/acyclic anti-shadow merge over an open, contract-admitted extension set plus its shared `jit-build`/`coordination` capability extensions; optional extension-astcheck app adapters contribute trusted UI-port handlers, while declarative apps and `UiSource` modules do not enter the linked-extension set | Authoritative target |
| [extension_conformance_doctrine.md](./extension_conformance_doctrine.md) | **The open-core contract.** amoebius admits extensions carrying arbitrary logic, so it cannot enumerate its compositions and instead fixes an algebra: one declared component per calculus (artifact · budget · lift · workflow · evidence), a conformance gate **generated from the declaration** rather than authored, a content-addressed **verdict seal** that alone admits an extension to a link set, and the link-time union closure argument by which *arbitrary* compositions of conforming extensions are themselves conforming. A hardware substrate is admitted by the same contract | Supporting slice |
| [extension_conformance_laws.md](./extension_conformance_laws.md) | **L1–L5** per extension (totality · determinism · budget honesty · scope propagation · evidence) and **C1–C7** over composition (closure · identity · associativity · non-interference · budget additivity · scope conjunction · name disjointness), each with its hazard, its author-facing guideline, and the mechanical discharge the generated gate performs. A slice of the doctrine above. | Supporting slice |
| [extension_conformance_security.md](./extension_conformance_security.md) | **S1–S6**: attestation as a type index, the **skolem scope** that turns a runtime tenant into a compile-time index, refusal as the default, indistinguishable refusal, injective derived namespaces, and honest revocation bounds — each with the seed observation that motivated it. A slice of the doctrine above. | Supporting slice |
| [extension_conformance_transactions.md](./extension_conformance_transactions.md) | **P1–P6**: Postgres's two roles split (a provisioned backend amoebius never reads versus the typed data plane it emits every statement of), no ORM or query builder, the scope as a required constructor field, schema and row policy derived from one declaration, scope-indexed results, and schema change as a typed transition. A slice of the doctrine above. | Supporting slice |

## Platform & cluster

| Document | Purpose | Document role |
|----------|---------|--|
| [platform_services_doctrine.md](./platform_services_doctrine.md) | The standard services every cluster runs (`registry:2` Distribution · MinIO · Vault · Pulsar · ephemeral Redis/Sentinel · Prometheus/Grafana · Percona/Patroni Postgres · Envoy/Gateway-API · Keycloak · LB) — the concrete providers/facilities; HA-always, Keycloak-owns-all-ingress | Authoritative target |
| [storage_lifecycle_doctrine.md](./storage_lifecycle_doctrine.md) | `no-provisioner` retained PVs (`<ns>/<sts>/pv_<n>`, sized, host/EBS-bound); the ephemeral-cluster-infrastructure / durable-backing lifetime split; deterministic rebind and shrink-without-data-destruction; the control-plane daemon holds no PVC (its state is the MinIO bucket) | Authoritative target |
| [migration_doctrine.md](./migration_doctrine.md) | The general migration law and its instance table — `create-new → verified-migrate → retire-old`, gated on an observed readiness edge, with no representable destructive verb, the old coordinate retained until an out-of-band privileged act, and rollback as a pointer CAS over the immutable ledger; one row per sense (spec diff · volume shrink · DB schema · topic retention · registry rehome · gateway handover/takeover · backup restore · browser record · UI release · tenant promotion · DSL schema), the two stated exceptions (`Failover` RPO>0, and the sibling-convergence homonym), and the two instances recorded **open** | Authoritative target |
| [inforcespec_migration_doctrine.md](./inforcespec_migration_doctrine.md) | How an `InForceSpec` evolves without ever *representing* destruction: ordered idempotent readiness-gated migrations; shrink as create-new → verified-migrate → retire-old (`ReclaimEligible`, with backing deletion an external privileged operator act rather than a spec verb or test-harness action); cross-tenant sharing as an append-only revocable capability edge | Authoritative target |
| [backup_recovery_doctrine.md](./backup_recovery_doctrine.md) | The backup & recovery surface: the closed `BackupPolicy` deployment rule (remote, append-only/WORM, air-gapped manual/automatic media); the write-but-never-delete credential boundary that keeps retention/deletion out of band; the bounded-no-overcommit sizing fold; the verified content-addressed `BackupArtifact`; restore that seeds a fresh coordinate (never overwrites); and the `ColdSeedFromBackup` down-primary posture that seeds a secondary and takes the gateway only after proven freshness — consistency over availability | Authoritative target |
| [namespace_layout_doctrine.md](./namespace_layout_doctrine.md) | The Kubernetes namespace partition: one derived namespace per platform capability + one per app, never hand-authored, with default-deny NetworkPolicy following the dependency graph | Authoritative target |
| [cluster_lifecycle_doctrine.md](./cluster_lifecycle_doctrine.md) | kind/rke2/provider clusters; bootstrap; **amoebic spawning** (parent/child); teardown-with-cleanup vs chaos-failover; push-back on unsatisfiable root `InForceSpec`; dynamic node provisioning | Authoritative target |
| [gateway_migration_doctrine.md](./gateway_migration_doctrine.md) | Moving the wild-ingress gateway between clusters: the typed `GatewayMigration = <Planned \| Failover>` taxonomy — a planned RPO=0 quiesce→drain→cutover handover (new `InForceSpec` or a `ScalingPolicy` home→cloud move) versus an availability-first forced failover — plus the client-rebind protocol that keeps a live session bindable | Authoritative target |
| [readiness_ordering_doctrine.md](./readiness_ordering_doctrine.md) | Event-driven bring-up sequencing: a dependent starts on a dependency's observed **readiness edge**, never an elapsed duration — the no-`AfterDuration`-arm `Readiness` gate, the derived acyclic bring-up DAG, the bootstrap-tier `discover`/`RuntimeWitness` gates (no timers), and the honest limit that the spec forecloses the *shape*, not the port's *liveness* | Authoritative target |
| [single_logical_data_plane_doctrine.md](./single_logical_data_plane_doctrine.md) | One data plane reached from many compute locations vs many data planes reached by gateway migration — remote elastic compute (spot ML) as **Pulsar/MinIO clients of the home cluster's one store over the fabric, not a second cluster**; a stretched host worker as the attach-pool shape (a data-plane client, not a member), distinguished from a member node and from a second cluster; the `DataPlane`/`FabricMember` binding that makes an unreachable store uninhabitable | Authoritative target |
| [cluster_topology_doctrine.md](./cluster_topology_doctrine.md) | The **declared** compute-engine axis: `ComputeEngine` (Kind/Rke2/**Managed EKS**), Linux-host witnesses for self-managed nodes, and explicit provider worker node classes/quotas for the hostless managed control plane; plus engine↔substrate compatibility, multi-substrate clusters, and stretched-cluster witnesses (a full external member on `Managed` still requires a provider-native hybrid arm) — [§3.13](../illegal_state/illegal_state_topology.md#313-a-compute-engine-incompatible-with-its-substrates-managed-providers-first-class)–[§3.16](../illegal_state/illegal_state_topology.md#316-a-multi-node-rke2-cluster-with-fewer-linux-hosts-than-nodes-or-a-host-reused) / [§4.7](../illegal_state/illegal_state_techniques.md#47-compatibility--topology-relations-by-construction-over-a-collection) | Authoritative target |
| [resource_capacity_doctrine.md](./resource_capacity_doctrine.md) | The complete provisioning model: pod/host/build/engine/fabric/controller/gateway/executor envelopes; pod/CNI/CSI slots; mapped/API/etcd state; logical pod ephemeral plus layout-routed OCI/cache storage; presentation-rounded durable/object/database/migration storage; provider quotas; accelerator count/profile/raw-reserved-allocatable/free VRAM or unified memory; fixed/elastic placement; and the private `ProvisionedSpec` required before `renderAll` — [§3.17](../illegal_state/illegal_state_capacity.md#317-an-over-committed-deploy-or-workload-host--vm--cluster-capacity-exceeded)/[§3.27](../illegal_state/illegal_state_capacity.md#327-a-deployment-that-fits-in-aggregate-but-has-no-resource-capable-placement)/[§3.30](../illegal_state/illegal_state_capacity.md#330-an-accelerator-memory-envelope-that-cannot-fit-the-selected-devices-or-unified-memory-pool), honestly decode-foreclosed | Authoritative target |
| [resource-capacity slices](./resource_capacity_types.md) | The capacity family's six slices, each owning one aspect the hub above routes to: [types](./resource_capacity_types.md) (the envelope vocabulary), [schema](./resource_capacity_schema.md), [sources](./resource_capacity_sources.md) (where a number may come from), [folds](./resource_capacity_folds.md) (the arithmetic), [storage](./resource_capacity_storage.md), and [construction](./resource_capacity_construction.md). | Supporting slice |
| [substrate_doctrine.md](./substrate_doctrine.md) | Hardware substrates (apple / linux-cpu / linux-cuda / windows), the universal `linux-cpu` lane at each host's natural architecture, pristine guests (Incus/Lima/WSL2), host worker nodes, and the authoritative capacity/device inventory; plus no-env/no-PATH tool ensure and the bounded Python pre-binary handoff | Authoritative target |
| [substrate_node_inventory.md](./substrate_node_inventory.md) | The node inventory: which hosts and substrates exist, what each advertises, its taints, and its site. A slice of the doctrine above. | Supporting slice |
| [apple_metal_headless_builds.md](./apple_metal_headless_builds.md) | The Apple-Metal host worker's headless, on-host build/run shape: Haskell declarations lazily render the `/usr/bin/clang`-built bridge and runtime MSL inputs beneath `.build/**`, with runtime MSL compilation via `MTLDevice.makeLibrary(source:options:)` — the hard **no-Tart / no-VM / no-keychain / no-full-Xcode / no-per-kernel-Swift-build** commitment and its rationale | Authoritative target |
| [image_build_doctrine.md](./image_build_doctrine.md) | Baked non-Registry service binaries plus the separately pinned and preloaded sole `registry:2` Distribution image; immutable versioning, snapshot-bound build admission, and the typed bootstrap-registry cycle-break | Authoritative target |
| [manifest_generation_doctrine.md](./manifest_generation_doctrine.md) | Generating the full k8s object set from pure typed Haskell — **no Helm, no third-party charts** — through the sole public `renderAll :: ProvisionedSpec -> [K8sObject]` over a sealed identity-keyed source set, preserving every checked resource projection and excluding action-owned root-ledger/Lease mutable fields from generic SSA; plus the snapshot-bound typed-action reconciler | Authoritative target |

## Secrets, identity, IaC

| Document | Purpose | Document role |
|----------|---------|--|
| [vault_pki_doctrine.md](./vault_pki_doctrine.md) | Vault as the fail-closed secrets root; root password-encrypted unseal; parent/child unseal + secret injection; the root PKI trust anchor; the SecretRef-by-name contract; the non-member host-worker Vault-auth-method seam (k8s-auth assumes cluster membership) | Authoritative target |
| [pulumi_iac_doctrine.md](./pulumi_iac_doctrine.md) | Haskell lazily renders Pulumi programs beneath `.build/**` for in-cluster execution only (MinIO backend + Vault-envelope encryption); DNS (route53) + TLS (zerossl); provider clusters; the surface-provider-capability-don't-build discipline (a provider-native arm such as EKS Hybrid Nodes is surfaced, never an amoebius-built second control-plane fabric); the EBS create-vs-delete credential model | Authoritative target |
| [pulumi_ebs_credential_model.md](./pulumi_ebs_credential_model.md) | The EBS create-vs-delete credential split and the deletion authority that follows. A slice of the doctrine above. | Supporting slice |
| [tenancy_doctrine.md](./tenancy_doctrine.md) | The first-class `TenantId` orthogonal to the cluster axis; issuer-qualified `SubjectSpec`, `Membership`, `Owner`, and `RoleBinding` values by which a valid `InForceSpec` cannot name a foreign subject/tenant resource; provider RBAC derived from the tenant→role graph; the tenant-admin surface as a scope-narrowed admin mutation | Authoritative target |

## Runtime, transport, determinism

| Document | Purpose | Document role |
|----------|---------|--|
| [daemon_topology_doctrine.md](./daemon_topology_doctrine.md) | The one Haskell runtime binary's contexts (command mode / sudo host daemon / in-cluster); bounded `pb/**` Python exists only before that runtime to ensure the toolchain, build, and `exec` the binary. The **control-plane daemon** uses a mandatory Kubernetes Lease for at-most-one-writer cluster/secret authority, with separate scheduler and worker roles from the same executable | Authoritative target |
| [host_cluster_comms_doctrine.md](./host_cluster_comms_doctrine.md) | Host↔cluster communication: a host compute daemon is a **Pulsar + MinIO peer over host-only NodePorts (no mTLS)**; host binary ↔ kubeapi via distro mTLS; the host-only network restriction ([§5.1](./host_cluster_comms_doctrine.md#51-the-generalization-localhost-or-the-authenticated-wireguard-fabric) generalizes it to the authenticated WireGuard fabric) | Authoritative target |
| [bootstrap_sequence_doctrine.md](./bootstrap_sequence_doctrine.md) | The ordered path from a bare host to a reconciling cluster and the **admin control plane** it hands off to: `pb/**` ends when it `exec`s the Haskell binary; the host-daemon→control-plane-daemon handoff follows, channel-1 kube-apiserver access is bootstrap-only, and subsequent operator commands are Haskell runtime responsibilities over the private admin channel | Authoritative target |
| [network_fabric_doctrine.md](./network_fabric_doctrine.md) | The inter-node/inter-cluster fabric: **raw kernel WireGuard configured by amoebius (not Netmaker)**, Vault-KV peer keys, rendered peer config, hub = gateway role; the `Networking = Gateway \| Vpn` sum with the `SecureGatewayReach` and `ControlPlanePeer` endpoint indices and the apiserver-VPN-IP render obligation for a single stretched cluster; the localhost→authenticated-fabric generalization; and the **no-Linkerd-for-v1** service-mesh verdict | Authoritative target |
| [pulsar_client_doctrine.md](./pulsar_client_doctrine.md) | The native-protocol Haskell Pulsar client (`amoebius-pulsar`, forked from supernova): TCP binary protocol, **no WebSockets**, the declarative topology algebra, at-least-once + dedup | Authoritative target |
| [ui_realtime_coordination_doctrine.md](./ui_realtime_coordination_doctrine.md) | Authenticated same-origin browser WebSockets, replicated stateless UI servers, ephemeral Redis connection ownership/cross-pod fanout, Sentinel failover, drain/reconnect, and durable cursor/receipt repair from Pulsar/providers | Authoritative target |
| [content_addressing_doctrine.md](./content_addressing_doctrine.md) | The content-addressed MinIO store, deterministic experiment identity, train→infer provenance, cross-substrate engine-family compatibility, and fine-tune/continuous-feed DAG — plus the per-node cache-owner shape whose residents/materialization fit `CacheBudget ≤ emptyDir.sizeLimit` and whose pod-ephemeral request reserves that volume plus writable/log headroom, exposed to clients through typed handles | Authoritative target |
| [content_addressing_determinism.md](./content_addressing_determinism.md) | Determinism by construction — pinned inputs, pure stages, and a derived seed — and the honest limit of each. A slice of the doctrine above. | Supporting slice |
| [monitoring_doctrine.md](./monitoring_doctrine.md) | Monitoring as a mandatory, non-vacuous property of every workflow and extension — the `WorkflowMonitor` SLO, per-topic `Liveness`, and per-extension `MonitoringSurface` (jitML → TensorBoard, baked + MinIO-backed) that make an unmonitored workflow/extension unrepresentable; the derived Grafana rules/panels + the `workflow-health` TableView operator read-model; the admin-global + delegated per-user `AccessScope` with no `Public` arm; the peer-rides-geo-replication + forest-**foreclosed** parent posture; the cluster-local-only Thanos role; and the type/decode/runtime honesty split | Authoritative target |
| [release_lifecycle_doctrine.md](./release_lifecycle_doctrine.md) | Target delivery as typed composition on gate-passed predecessor primitives — **no external CI/CD control plane** (no Argo/Flux/Tekton): the immutable `Release` ledger (`releaseHash`), the per-`Environment` (Dev/Staging/Prod) ETag-CAS promotion pointer, the `PromotionGate` that makes promote-unverified→prod unrepresentable, and the readiness-gated `RolloutPlan`/`RolloutPhase` apply (schema-migration as a phase, canary, rollback) on the in-cluster SSA reconciler | Authoritative target |

## Verification

| Document | Purpose | Document role |
|----------|---------|--|
| [chaos_failover_doctrine.md](./chaos_failover_doctrine.md) | The Extract→Model→Inject methodology + the proven/tested/assumed ledger; the invariant-confluence "Second Axis" that governs **async cross-cluster geo-replication & failover** — the one place a per-system proof obligation concentrates (intra-cluster consensus is delegated to MinIO/Pulsar/Postgres/Patroni). Generalized from prodbox's `chaos_hardening_doctrine.md` | Authoritative target |
| [chaos/failover slices](./chaos_failover_second_axis.md) | The failover family's two slices: [second axis](./chaos_failover_second_axis.md) (invariant confluence) and [worked examples](./chaos_failover_worked_examples.md). | Supporting slice |
| [consistency_pacelc_doctrine.md](./consistency_pacelc_doctrine.md) | The whole-stance **PACELC posture** stated once: in-cluster PC/EC, Vault C-over-A, odd-quorum consistency, and cross-cluster **Else=Latency** (async-only) are fixed by construction; the async cross-cluster **gateway** is the one boundary that trades consistency for availability, and the `InForceSpec` carries only its bounded magnitudes — the R8 lag bound, the R9 RTO budget (RPO derived = lag), the parent-owned pairing + low DNS TTL, and the per-app `SingleCluster \| GeoReplicated` flag; the mode is world-triggered, not authored | Authoritative target |
| [testing_doctrine.md](./testing_doctrine.md) | **Owns the register definitions** — three phase-gate registers (1 pure with independent Haskell expectations · 2 boundary-with-fakes · 3 live) plus the Register-2.5 deterministic-simulation activity, which is never a phase gate; tests are Haskell-declared topologies, serialized artifacts are lazy `.build/**` output, and `suggest-test` emits only an external/untracked proposal | Authoritative target |
| [testing_spoof_resistance.md](./testing_spoof_resistance.md) | Spoof-resistant evidence: a gate observes an unforgeable effect produced after it started, never a value the system under test supplied. A slice of the doctrine above. | Supporting slice |
| [test_derivation_analysis.md](./test_derivation_analysis.md) | The analysis behind the derivation boundary: Haskell generates coverage enumeration, a separately authored Haskell module supplies expectations, and every serialized test artifact is lazy `.build/**` output | Authoritative target |
| [formal_model_doctrine.md](./formal_model_doctrine.md) | The model-as-data pattern: one reifiable Haskell `Model` renders both the runtime decision function (`interpret`) and the generated, never-committed TLA+ (`emitTLA`), eliminating the hand-maintained correspondence table while differential tests check renderer faithfulness | Authoritative target |
| [gateway_migration_model_doctrine.md](./gateway_migration_model_doctrine.md) | amoebius's **one** simulation/proof obligation — the cross-cluster **gateway migration**, both `Planned` and `Failover` branches — as a `Model`, simulated (io-sim) and proven (TLC), safety **and** liveness-under-fairness: one-and-done + a per-`InForceSpec` decode-time structural-fit fold with a pairwise-independence cutoff | Authoritative target |
| [deterministic_simulation_doctrine.md](./deterministic_simulation_doctrine.md) | **Register 2.5** — deterministic simulation testing (DST): the real daemon/reconciler code, lifted onto `io-classes`, runs under `IOSim`/`IOSimPOR` against a modeled fault-injectable environment (Pulsar/MinIO/apiserver/route53/Vault/clock), so concurrent schedules and injected faults can be explored in-process and deterministically replayed before live evidence; the modeled-environment-fidelity premise remains explicit | Authoritative target |
| [tla_modelling_assumptions.md](./tla_modelling_assumptions.md) | **Superseded** — a redirect stub to [gateway_migration_model_doctrine.md](./gateway_migration_model_doctrine.md) and [formal_model_doctrine.md](./formal_model_doctrine.md) | Redirect only |

## Method & cross-cutting

| Document | Purpose | Document role |
|----------|---------|--|
| [conformance_harness_doctrine.md](./conformance_harness_doctrine.md) | The no-cluster conformance spine: how amoebius *uses* the registers for pre-cluster validation (definitions owned by [testing_doctrine.md §2](./testing_doctrine.md#2-the-registers-of-amoebius-testing)) and the invariant that rendering a plan / `--dry-run` must never require live infrastructure | Authoritative target |
| [validation_frame_doctrine.md](./validation_frame_doctrine.md) | Later container replay checks portability of an already accepted language/runtime result; it is never a prerequisite or oracle for the hardware-free DSL gate barrier. | Supporting slice |
| [generated_artifacts_doctrine.md](./generated_artifacts_doctrine.md) | Every non-Haskell behavioral artifact is emitted lazily from Haskell beneath `.build/**` and never committed | Authoritative target |
| [repository_layout_doctrine.md](./repository_layout_doctrine.md) | The closed tracked-source grammar: behavioral source is `.hs`, bounded `pb/**` Python is the sole exception, operator inputs are untracked, and all generated/evidence/runtime bytes stay in the contained roots | Authoritative target |
| [diagram_conventions.md](./diagram_conventions.md) | The two Mermaid registers — algebra and orientation — delegated to by [documentation_standards.md §7](../documentation_standards.md#7-diagrams): node **shape** = functional-programming role (value/fold/gate/effect/decision/accumulate/seal/refuse), node **colour** = honesty / validation-locus band (proven-in-sibling / design-intent / runtime-checked) in the algebra register, no amoebius-defined styling in the orientation register; the canonical `classDef` header, both legends, the decision rule, the quota, and the authoring + anti-pattern rules | Authoritative target |
| [lift_and_compose_doctrine.md](./lift_and_compose_doctrine.md) | **Self-containment.** amoebius depends on no seed project and no seed depends on amoebius; the five seeds (`hostbootstrap`, `prodbox`, `jitML`, `infernix`, `mattandjames`) are **reference implementations** whose pure structures amoebius re-derives, admissible only once the guarantee amoebius adds has been named. Carries the five-row re-derivation map, the independent-convergence evidence, and the **lift calculus** — a closed layer set, a total transition relation, and a witness per transition | Supporting slice |
| [jit_artifact_doctrine.md](./jit_artifact_doctrine.md) | **The artifact calculus.** Every non-Haskell behavioral artifact is generated lazily from Haskell; `pb/**` is the sole source-language exception; targets, recipes, content addresses, regions, and Haskell semantic oracles replace tracked byte goldens | Supporting slice |
| [jit_budget_doctrine.md](./jit_budget_doctrine.md) | **The budget calculus.** The storage grant as the authority a byte needs to exist, a ceiling inseparable from the concurrency it is shared across, `admit`/`admitFirst` refusing before a partial write, and the reaper every retained artifact must name | Supporting slice |
| [workflow_calculus_doctrine.md](./workflow_calculus_doctrine.md) | **The workflow calculus.** Provision · build · deploy · observe · teardown as arms of one algebra; teardown as a **type obligation** discharged by tearing down or by explicit transfer, specified so that a workflow ending while it holds one is rejected at compile time; test-as-workflow; and the self-referential suite in which amoebius's own gates are workflows | Supporting slice |
| [evidence_calculus_doctrine.md](./evidence_calculus_doctrine.md) | **The evidence calculus.** The fifth declared component: every claim names the one fixture that would falsify it, every unrepresentability claim names a compile-fail fixture pinned to its reason, and a claim may be stated no more strongly than its fixture kind allows — compile-fail, property, oracle, or live probe. Fixes why an expectation is authored from the requirement rather than derived from the output it checks | Supporting slice |

## Related Documents
- [Development Plan](../../DEVELOPMENT_PLAN/README.md)
- [Documentation Standards](../documentation_standards.md)
