- vpn and linkerd service mesh story (certs ? ) [RESOLVED — raw kernel WireGuard configured by amoebius (Vault-KV Curve25519 peer keys, rendered peer config, hub = gateway role); NOT Netmaker (a duplicate control plane — the Harbor/Helm of networking). Linkerd deferred/rejected for v1 (redundant for a Pulsar-async architecture; gateway-migration traffic-split comes from Gateway-API HTTPRoute weights). See documents/engineering/network_fabric_doctrine.md]
x pulsar payload ? protobuf ? something more functional like CBOR ? does dhall make sense ? (probably not a dense binary format) [RESOLVED — Pulsar payloads are exclusively CBOR (canonical, self-describing, dense binary); protobuf and JSON are rejected as wire formats and Dhall stays desired-state config, never a payload. The produce surface takes only a Serialise/CborPayload value — no produceRaw/JSON/protobuf constructor — so a non-CBOR payload is grade-(1) uninhabitable. See documents/engineering/pulsar_client_doctrine.md §3.1; illegal_state_catalog.md §3.23]
x semantics for full pulsar topic lifecycle management, including teired storage (bound topic sizes, movement to s3 mandatory after some amount of time, user sets retention policy; no way to represent keeping more storage than you have room for. unbounded minio only possible if there are autoscaling) [RESOLVED — the topic storage lifecycle is bounded, tiered and retained: hot-tier sizes are explicit and hard-capped, offload to object storage is mandatory past the retention edge, the user sets the retention policy, and the hot tier can never overflow. Unbounded storage is unrepresentable — host storage is physical and cloud storage is quota-capped, so no app can claim more than exists (autoscaling itself is still open, see line 4). See documents/engineering/pulsar_client_doctrine.md §6.1; storage_lifecycle_doctrine.md §5, §5.2]
- resource scaling semantics. autoscaling, compute price shopping
- can a host binary's dhall ever get changed ? i think only the parent should do that [RESOLVED — only the parent mints a frame's .dhall; the child receives it read-only at frame entry (streamed on stdin, written once before exec) and has no verb to rewrite its own config. See documents/engineering/dsl_doctrine.md §3]
- test dhall generating topology
- should infernix/jitML be extensions ? what about spectralmc ? [RESOLVED for infernix/jitML — yes: each nests as a typed ExtensionSpec (Path 1, linked-not-loaded), merged into the one binary at compile/link time; the v1 extension set is closed at {infernix, jitML, mattandjames}. SpectralMC is DEFERRED to a future pass — it would enter later via Path 1 or the Phase-15 extension DSL, and is NOT resolved here. See documents/engineering/dsl_doctrine.md §4]
- do infernix inference engines download / build their engines at startup from dhall ? [RESOLVED — no: inference engines are BAKED into the base image, never downloaded or built at startup. EngineRuntime is a closed substrate-tagged union with no Url/Download/Fetch arm (selected by substrate, not fetched), so the .dhall can never author a download — grade-(1). This is tier 1 of the three-tier ML-asset lifecycle (engine baked, model staged, kernel JIT'd). See documents/engineering/content_addressing_doctrine.md §4.5; service_capability_doctrine.md]
- no tart ! [RESOLVED — committed to doctrine: Apple-Metal builds are headless on-host via a fixed clang-built Metal bridge, no VM. See documents/engineering/apple_metal_headless_builds.md]
- do we actually need pulumi ? can our state be the dhall just as it was with helm ? [RESOLVED — keep Pulumi for v1. The Helm analogy breaks on an asymmetry: Helm sat on k8s, which already reconciles, so dropping it just means rendering better-typed inputs; cloud APIs (EKS/EBS/route53/ACME) have no built-in reconciler, so Pulumi *is* the diff/state/destroy engine and is far harder to replace. Dhall stays desired-state; Pulumi's checkpoint is the honest wart (a stored second state store §8 argues against), contained by carving the high-churn spot fleet into a bespoke checkpoint-free tag-discovery reconciler. Crossplane is OUT (see line 16: continuous parent-cluster controllers, k8s-Secrets vs Vault, harder to prove). See documents/engineering/pulumi_iac_doctrine.md §0]
- specifics for how rke2 clusters are rolled out, including control plane and worker nodes [RESOLVED — rke2 is a typed server/agent split: Rke2Servers is a closed union over the legal odd etcd quorums {1,3,5} (an even/zero no-quorum control plane has no constructor — grade-(1)), agents are a List, distinctness runs over servers ∪ agents (grade-(2) fold), and the control-plane taint is derived. Rollout is a reconcile, not a state machine: the first server cluster-inits and mints the node-token; further servers and all agents join by server-URL + token with config rendered read-only by the parent, and rejoin is idempotent. See documents/engineering/cluster_topology_doctrine.md §2, §4; cluster_lifecycle_doctrine.md]
- amoebius extensions, eg spectralMC ? [RESOLVED (seam only) — the amoebius extension seam is the typed ExtensionSpec: v1 ships Path 1 (linked, closed at {infernix, jitML, mattandjames}) and reserves Path 2 (the deferred Phase-15 Haskell extension DSL) for a non-vendored third party. The seam is provisioned but SpectralMC itself is DEFERRED — no SpectralMC-specific wiring exists yet. See documents/engineering/dsl_doctrine.md §4]
- story for migrations / CICD [RESOLVED — no external CI/CD control plane (no Argo/Flux/Tekton): delivery is typed composition on primitives amoebius already owns. A Release is an immutable ledger entry (releaseHash); an Environment (dev/staging/prod) is an ETag-CAS pointer to a Release; promotion is a pointer CAS gated by a PromotionGate (promote-unverified→prod is grade-(1) unrepresentable); a RolloutPlan is an ordered readiness-gated apply on the in-cluster SSA reconciler, with DB schema-migration as a RolloutPhase. See documents/engineering/release_lifecycle_doctrine.md]
- make it impossible to represent illegal deployments, eg rke2 on an apple host without a vm, a multi-node rke2 cluster on a single machine, a workload deployment needing more memory/storage/cpu resources than is available on the cluster, an rke2 or kind deploy needing more resources than are available on the host, a vm deploy needing more resources than are available on the host. a compute engine not compatible with available substrates (multi-supstrate clusters are allowed). taints tolerations or node affinity rules that stop pods from being run at all. multi-node kind clusters can only be deployed on single linux machine/vm/wsl2 instances. multi-node rke2 clusters can only be deployed on as many linux machine/vm/wsl2 instances. we need an explicit pure pulsar topic lifecycle managemnt policy, including resource availability and multi-tier storage. unbounded storage is never acceptable, we either have host level storage based on physical machines, or cloud storage capped by quotas. either way it is impossoble to represent an application that can consume more storage than is available. this goes for pulsar and minio. dynamic resource provision is acceptable, and amoebius owns dynamic resource logic. it can be arbitrary, including capacity based as well as instance pricing. we also support eks as a first class citizen. [RESOLVED — the enumerated illegal deployments are eliminated at their honest grades: node cardinality (an even/zero-server rke2 control plane, multi-node kind across >1 machine, multi-node rke2 without enough Linux hosts) and rke2/kind on an Apple host with no Linux node/VM are grade-(1) unrepresentable; resource over-commit (a deploy needing more memory/storage/cpu than the host or cluster provides) is a grade-(2) total decode-time rejection (Dhall has no dependent arithmetic); an engine incompatible with every available substrate is the grade-(2) compatibility relation (multi-substrate clusters still allowed); unbounded pulsar/minio storage is grade-(1) via the closed storage-backing/budget unions; EKS is a first-class substrate; an unverified environment promotion is grade-(1) via the PromotionGate. Dynamic/price-aware provisioning remains open — see line 4. See documents/engineering/illegal_state_catalog.md; resource_capacity_doctrine.md §1-§5; cluster_topology_doctrine.md §2-§5; storage_lifecycle_doctrine.md §5; pulsar_client_doctrine.md §6.1; release_lifecycle_doctrine.md §4]
- incorporate the new <prodbox>.dhall child generation method (recent commits) [RESOLVED — child <project>.dhall delivery folded into doctrine as in-place stdin streaming (parent mints; child never rewrites; in-cluster pod keeps ConfigMap); frame-descent vs cluster-spawn transports distinguished. See documents/engineering/dsl_doctrine.md §3, §5; cluster_lifecycle_doctrine.md §2-§3; daemon_topology_doctrine.md §6; manifest_generation_doctrine.md §2]
- i think crossplane should be out it's too opinionated (requires parent clusters to run continuously, uses k8s secrets which are at odds with our vault centric secrets policy)

Jul 4 review

- documentation seems a little too colloquial at points "The instinct from a decade of bash and Helm templating is to put the how in the config" this overly dramatizes the problem it seeks to solve 


a cluster is the consistency boundary--good. but in jitML it is impossible to reperesent inference with an untrained model. and a trained model is an artefact guaranteed to be reproducible, even though it is only reproducible on similar hardware. does that mean each cluster must train its own models, since that is the consistency boundary ? does that mean 

- have we made it impossible to represent deployment readiness races. we don't want arbitrary wait periods till a port becomes responsive, we want event driven sequences 

- how does a new dhall get implemented or unlock keys provided in the root node ? does the project binary provide a thin cli tool to interact with the amoebius daemon api or something else ?

- to what extent does prodbox need to be kept in alignment with amoebius doctrine ? should infernix/jitML be migrated to hostbootstrap before amoebius ? is daemon-substrate still worth doing ?

- when the host daemon is bootstrapping a cluster, what is the exact sequence, and handoff of control to the cluster daemon ? 

why not have the host daemon have it's own rest unseal endpoint, reachable by nodeport, which takes the password and performs the remaining steps ? the main shape we want here is that the host binary only directly interacts with the cluster and k8s control plane during initial bootstrap, including deploying the host daemon. once all services are up and running (even before vault init) all further interactions occur through the prodbox-daemon service, a nodeport

- are we fully leveraging pulsar to avoid race conditions and cluster-level leadership elections more robustly handled by pulsar ?

- would it make sense to build and validate / simulate the entire dsl before implementing anything else ?
